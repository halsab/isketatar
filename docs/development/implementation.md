# Реализация курса

Начало: 12 сентября 2026 года. Основание — [D0–D12](../production/11-development-roadmap.md). Новое поручение владельца разрешает разработку и локальные коммиты; прежнее ограничение «только документация» относится к завершённому этапу проектирования. Push и публичный релиз не выполняются.

Каждый этап и выделенный подэтап выполняется последовательно: границы → необходимые проверки → реализация → независимое ревью с чистым контекстом → исправления → локальный коммит. Тесты прежде реализации применяются к предметным правилам, транзакциям и отказам; для вёрстки используются проверки браузером. Ревьювер читает спецификацию и diff самостоятельно, не получает объяснений автора реализации.

## Очередь и границы

| Этап | Область | Приёмка / зависимость | Состояние |
|---|---|---|---|
| D0 | Этот журнал, замороженный корпус, внешние условия | Валидаторы, hashes, список условий | Проверки пройдены; внешние условия ожидают подтверждения |
| D1 | package/lockfile, app bootstrap, base path, CI build | Типы, сборка, hash deep link; D0 | Завершён |
| D2 | domain/content, domain/learning, сборка каталога | 30 learning-cases, полный корпус; D1 | Завершён: D2.1 и D2.2 |
| D3 | data/progress, импорт/экспорт, IDB | Атомарность, CAS, помощь, восстановление; D2 | Завершён: D3.1–D3.3 |
| D4 | ui, styles, шрифты и лицензии | Токены, RTL, глифы, адаптивность; D3 | Инженерная часть завершена: D4.1 и D4.2; реальные AT/устройства — D9 |
| D5 | lessons/practice/reader, сквозной срез | V04, B-материал, A02, READ-01/03; D4 | Завершён: D5.1–D5.3 |
| D6 | Все уроки, диагностика, итог, результаты | 53 урока, 436 Q, D/F/RQ; D5 | Ожидает |
| D7 | reader/dictionary/reference/review/settings | Каталоги, доступ, поиск, SRS; D6 | Ожидает |
| D8 | PWA, пакет, установка, обновления | Offline и несколько вкладок; D7 | Ожидает |
| D9 | Браузерная/отказная приёмка, бюджеты | Матрица и честные ограничения; D8 | Ожидает |
| D10 | Редактура, рецензия, пилот | Внешние свидетельства; D9 | Ожидает участников |
| D11 | CI/CD, кандидат, smoke/rollback | Сборка на push; без push и публикации | Ожидает |
| D12 | Инструкции сопровождения, обновления | Процедуры; эксплуатация после выпуска | Ожидает |

## Экраны и сценарии

Точные ID/маршруты — [05](../production/05-routing.md), состояния — [06](../production/06-screen-specs.md). Учебные переходы — [03](../production/03-learning-mechanics.md), ввод/фокус — [07](../production/07-interaction-accessibility.md), офлайн — [08](../production/08-pwa-platforms.md), сохранность — [09](../production/09-security-reliability.md).

| Покрытие | Этап реализации |
|---|---|
| S00–S02: запуск, знакомство, главная | D1, D5, D6 |
| S03–S08: программа, урок, упражнения, результаты, диагностика, итог | D5, D6 |
| S09–S10: очередь повторения, сеанс и результат | D7 |
| S11–S12: каталог чтения, читалка | D5, D7 |
| S13–S15: поиск, словарная статья/источник, справочник | D7 |
| S16–S18: настройки, перенос, сведения/помощь/приватность | D3, D7 |
| S19: ошибки и редкие состояния, включая офлайн | D1, D3, D8, D9 |
| Доступ из URL, поиск/ARIA, помощь из другой вкладки | D2, D3, D6, D7, D9 |
| Импорт, quota, restart, поздний callback, update/reload | D3, D8, D9 |

## D0: исходная база

Базовый commit `0f02e19`, content version `2026.09.11.1`. [baseline.json](baseline.json) фиксирует исходный manifest, обязательные ресурсы и снимок текста учебника. Все 31 обязательный JSON проверены по SHA-256 и размеру. Объём обязательного пакета — 2 131 151 байт. Записанного аудио нет; незаписанное аудио не выводится как доступный control.

`python3 tools/validate_content.py`: 0 ошибок, 53 урока, 145 правил, 361 пример, 436 вопросов курса, 540 исходных словарных записей, 12 текстов. `python3 tools/validate_production_docs.py`: pass, 0 ошибок. Это проверка исходников и спецификаций, не приложения.

Git remote: `https://github.com/halsab/isketatar.git`. Технический Pages owner — `halsab`, base path — `/isketatar/`. Публичное имя и контакт проверяются перед выпуском по [условиям](release-conditions.md). Существующие `.DS_Store` не являются результатом разработки и не включаются в коммиты.

UI-copy: 194 исходные строки + 72 добавления / 8 замен = 266 строк после переноса. Подготовленный план остаётся источником переноса, а не доказательством контекстной вычитки.

Агрегатный `corpus_sha256`: SHA-256 от UTF-8 сериализации массива `files` через `json.dumps(files, ensure_ascii=False, sort_keys=True, separators=(',', ':'))`, без завершающего LF. Пути отсортированы; каждый объект содержит path/sha256/bytes.

Независимое ревью D0: воспроизведены валидаторы и все 33 hashes; исправлена привязка S09–S19 к экранам, уточнены алгоритм hash и объём татарской вычитки. Инженерная приёмка D0 завершена; внешние условия остаются в отдельном реестре.


## D1: основа и сборка

Закреплены Node 24.21.0 LTS, npm 11.9.0 и точные версии зависимостей в package.json/lockfile. Добавлены React/Vite/TypeScript strict + noUncheckedIndexedAccess, HashRouter, base `/isketatar/`, татарские noscript/recovery, строгая production CSP и проверки границ модулей/состава dist. CI выполняет проверки и сборку без deploy; официальные actions проверены по Git refs и закреплены полными SHA. PR имеет только contents:read.

Проверки на Node 24.21.0: оба исходных валидатора, module boundaries, TypeScript, 7 unit assertions, production build; initial JS 82.96 kB gzip. Во встроенном Chromium открыта и перезагружена прямая `/isketatar/#/about`; закреплённый Playwright smoke проверяет ссылку/reload/переход домой. `npm audit --omit=dev --audit-level=high`: 0 известных уязвимостей. Shell этого этапа не является завершённым курсом; шрифты/экраны/прогресс/worker добавляются последовательно.

Независимое ревью D1: исправлена блокировка CSS Vite в dev, повторно проверены styles/console, production CSP, bootstrap/recovery и hash-навигация. Открытых замечаний нет; этап допущен к локальному коммиту.


## D2.1: типизированный каталог и ревизии

Границы: domain/content, data/content, сборочные инструменты/JSON Schema, runtime-каталог строк. Источники проверяются по исходным hashes и отдельной source schema; runtime имеет закрытые схемы и явную проекцию. Ajv standalone генерируется при сборке, без compile/eval в браузере. 266 татарских строк перенесены в src/ui/tt.json; внешняя вычитка остаётся в реестре.

Каталог разделён на core, восемь модулей, readings, assessments, dictionary и references. Репозиторий загружает нужный раздел, проверяя MIME, размер потока, SHA-256, UTF-8 и schema; неуспешный запрос можно повторить. Archive исключён до пользовательского каталога. Стабильные IDs, null readings и исходная графика сохранены; координаты слов последовательные UTF-16. Grading revision учитывает разрешённый читаемый контекст, но не explanation. Таблица разрешённых арабских presentation forms зафиксирована явно и используется только для ключей знакомства/поиска.

Обнаружено исходное различие source_sections у 11 уроков; адресное уточнение в 02 сохраняет объединение ссылок программы и полного урока. Исторические формы и ключи не менялись. Сборочный release manifest содержит версии, политики, revisions и hashes всех ресурсов из allowlist; собственный hash не включает. Время берётся из SOURCE_DATE_EPOCH или commit timestamp, ID включает время и bytes; неизменяемые URL выпуска подключаются в D8.

TDD: каталоговый тест сначала завершился ошибкой отсутствующего адаптера, затем стал проходить на полном корпусе. Проверены количества, A02, word ranges/READ-03, null/архив, LC-28, schema rejection и повреждённая загрузка. Учебный reducer, оценки и остальные LC относятся к следующему D2.2; сохранённой истории пока нет.

По независимому ревью исправлены ключи знакомства с отдельными словами H03, включение разметки слов в ревизию строки, проверка обеих границ графемы и явная проекция вложенных объектов без редакторских полей. Помимо закрытой JSON Schema проверяются допустимые ключи ответов, сложность, уникальность IDs, порядок координат и внутренние ссылки, включая принадлежность каждого урока модулю. Несовместимая схема/политика отличается от повреждённых bytes. Зафиксированы регрессионные примеры: 36 unit tests, TypeScript, границы модулей, исходные валидаторы и production build проходят. Повторное независимое ревью: открытых существенных замечаний нет, D2.1 допущен к локальному коммиту.

## D2.2: чистые учебные правила

Добавлены типы сохраняемых учебных записей и чистые функции проверки четырёх видов ответа, создания Attempt, учёта помощи и знакомства, допуска учебной лексики, расчёта урока/маршрута, диагностики/итога, SRS и словарного поиска. Нормализация ответа изменяет только предусмотренные пробелы и регистр кириллицы. Поиск имеет отдельные правила и не участвует в оценивании.

Подготовленные LC-01–LC-30 проверяются на уровне предметных правил: полные рубрики, первые отправки, независимость, revisions, origin/dedup расписания, доступ по связанным вопросам, null readings и все поисковые случаи. Дополнительные границы включают незавершённый новый цикл, downgrade после полного цикла, сохранение first_mastered_at, старую revision, suspended, пустые/управляющие символы, отдельную группу близких написаний и татарскую сортировку. Всего 85 unit tests с учётом D1/D2.1; исходные валидаторы, границы модулей и TypeScript проходят.

Это ещё не свидетельство durable-сохранения: транзакционная идемпотентность LC-23, reload/help LC-29, атомарное массовое завершение и перенос старой истории LC-28 дополнительно проверяются через хранилище в D3 и браузерные сценарии D5–D9. В D2 нет DOM, IndexedDB, сетевых запросов, получения времени или случайных IDs внутри учебных функций.

Независимое ревью D2.2: устранён обход помощи при переводе часов назад. Сохранённый перед отправкой факт помощи не сравнивается с показаниями часов; старые Attempt неизменны. Проверены все явные ключи 494 вопросов и все 53 полных самостоятельных цикла. Повторное ревью, 49 тестов учебного ядра, общий набор 85 тестов, TypeScript и production build проходят; существенных открытых замечаний нет. D2 завершён.

## D3.1: транзакции и учебные команды

Границы подэтапа: восемь stores, IDB-адаптер, согласованный snapshot, CAS, writer takeover, start/pause/resume/restart, замороженные планы, показ/помощь/draft/submit/ack/retry, массовое завершение диагностики/итога, SRS, настройки/закладки/позиции и очередь ввода. Импорт, экспорт, закрытые схемы сохраняемых записей, legacy, reset и отдельная ветвь памяти составляют следующий D3.2.

Каждая команда проверяет generation/epoch и предметные revisions внутри общей транзакции. Уведомление содержит только generation/state_revision и отправляется после completion. Помощь из любой вкладки устанавливает необходимые флаги до раскрытия материала; reveal неотправленного ответа остаётся командой владельца, создающей unknown Attempt. Очередь сохраняет выбор сразу, текст после 300 мс; поддерживает IME и явный flush, не присваивает старому callback новые полномочия после конфликта. Счётчики истории обновляются дельтами в той же транзакции.

Первый набор из 11 тестов адаптера и очереди прошёл на fake-indexeddb. Затем добавлены регрессии CAS карточки, старой grading revision, update gate, старого разбора, повторного показа/отправки при paused-проверке, часов и закрытия соединения во время commit. Для настоящего браузера подготовлен изолированный интеграционный сценарий с двумя IDB-соединениями, повторной отправкой, помощью, takeover, массовым итогом и abort; он удаляет только собственную временную БД.

Независимое ревью обнаружило обход помощи через прежний урок, остановку после возврата к просмотренному разбору, конфликт при открытии завершённого итога и ложный отказ после успешного commit при закрытии канала уведомлений. Исправления внесены; повторное статическое ревью дало допуск без существенных открытых замечаний. Полный набор из 108 тестов, TypeScript, границы модулей, исходные валидаторы и production build проходят. Настоящий браузер подтвердил шесть инвариантов IDB; закреплённый Playwright-набор из двух сценариев также проходит. Runtime-проверки разрешены владельцем для isketatar.

## D3.2: перенос и восстановление

Добавлены закрытые схемы записей и обменного конверта, сборочная генерация Ajv standalone, структурная проверка чтения/записи IDB, согласованный экспорт, проверка файла и предварительный просмотр, атомарная замена и reset. Импорт проверяет размер до чтения, UTF-8, глубину и опасные ключи, уникальность и внутренние связи, доступные точные revisions и политики. Stack обхода ограничен глубиной, широкий массив не создаёт отдельного кадра для каждого элемента. Лимит сырого ответа — 4096 UTF-16 units; производный NormalizedAnswer имеет отдельный запас для NFC (16384 units на строку, 4097 частей).

Завершённые оценки с доступными grading/normalization перепроверяются; изменение одной политики поиска не уничтожает их. Неизвестная revision/политика и зависимые записи сохраняются в legacy. Актуальный сеанс не может ссылаться на предъявление только из legacy. Закреплённый, но недоступный незавершённый выпуск сохраняется историей; совместимый active импортируется как paused. Импорт и reset создают новые generation/epoch; прежние callbacks и изменившийся preview отвергаются. Отказ после очистки stores откатывает всю транзакцию, предыдущий набор остаётся целым. Время ручного добавления/suspend не угадывается из часов: статистика SRS проверяется на поддержку первыми ответами, включая последний учтённый Attempt.

D3.2 создаёт первый поддерживаемый обменный формат; D3.1 был локальным промежуточным кодом без экспорта и без подключённого интерфейса прогресса, публичных файлов/миграций этого подэтапа нет. Снимок Attempt теперь включает фактическую помощь строки до отправки, поэтому помощь последующего retry не меняет прежний результат при переносе. Физическая БД и progress schema первого выпуска имеют версию 1; более новые неподдерживаемые версии отклоняются без удаления БД.

Независимое ревью исправило пустой импортированный set, смешение current/legacy ссылок, UTF-16 и расширение NFC, поддержку счётчиков SRS и совместимость политик. Целевой независимый прогон: 51 тест в четырёх файлах; допуск дан без открытых существенных замечаний. Браузерный harness подтвердил десять инвариантов, включая перенос и reset; Playwright — 2/2. TypeScript, исходные валидаторы, границы модулей и production build проходят. Следующий подэтап D3.3 — отдельная несохранённая ветвь памяти и её ограничения.

## D3.3: несохранённая ветвь памяти

Добавлен атомарный MemoryBackend с теми же проверками записей и команд. Явное переключение создаёт отдельную generation и приостанавливает скопированные сеансы; исходный репозиторий перестаёт принимать учебные записи. При недоступном снимке можно начать пустую ветвь. Экспорт сохраняет её в стандартном формате; импорт и очистка памяти не меняют исходную БД. Мягкий durable-предел не мешает временной работе, жёсткий предел переносимого файла сохраняется.

Известная незавершённая диагностика/итог в исходной БД защищает раскрытие помощи и после переключения, импорта или очистки памяти. Флаг сначала фиксируется в исходной БД; при невозможности записи раскрытие откатывается. Закрытое соединение открывается временно только для этой операции. Снять защиту после сброса можно по подтверждённой новой generation реального durable-reset, а не по одному флагу интерфейса.

Независимый ревьювер с чистым контекстом обнаружил потерю защиты при импорте проверки и закрытии до следующего snapshot. Исправление обновляет известную защиту после успешного commitImport; регрессия закреплена. Повторное независимое ревью: 55/55 целевых тестов, существенных открытых замечаний нет. Полный check: 140/140, TypeScript и исходные валидаторы проходят. Реальный Chromium подтвердил 12 инвариантов хранилища, включая durable-защиту и изоляцию памяти; Playwright — 2/2, production build проходит. D3 завершён; интерфейс переключения подключается в следующих этапах.

## D4.1: локальная типографика

Перенесены 46 цветовых значений двух тем и независимые размеры текста/арабицы в CSS. Полные Inter Variable и Noto Naskh Arabic Regular преобразованы из проверенных TTF без поднабора; OFL и copyright включены в release allowlist. Воспроизводимый инструмент `tools/prepare-fonts.py` принимает каталог исходных TTF, сначала проверяет исходные hashes, затем сравнивает cmap и shaping после WOFF2. Итоговые hashes/версии/размеры — [font-build.json](font-build.json). Это однократный инструмент подготовки, не новая тяжёлая зависимость CI.

Проверено 1200 различных арабских фрагментов текущего runtime/fixtures: нет .notdef, glyph IDs/кластеры/advance/offset совпадают до и после упаковки. 73 кириллические кодовые точки пользовательских данных/строк покрыты. Два шрифта занимают 452468 байт. Эта проверка не заменяет предметное сравнение начертаний на реальных устройствах.

ArabicText сохраняет исходный логический текст в bdi с tt-Arab/rtl, обычным весом и ink. ArabicFontGate проверяет загруженный объект ожидаемого FontFace через load/check; при отказе оценочный control и стимул отсутствуют, доступны сообщение и повтор. Независимые размеры/тема применяются data-атрибутами без inline style, CSP остаётся строгой.

Реальный Chromium: все строки fixtures, переносы на 320/390/768/1440 CSS px, 48px арабица с 24px кириллицей, отказ шрифта и успешный retry. Встроенный браузер открыл матрицу; визуально просмотрены светлый широкий и тёмный 320px рендеры: marks не обрезаны, смешанные скобки/числа и слова сохраняют порядок. Playwright 4/4, TypeScript, исходные валидаторы и production build проходят. Физические устройства, 200%/400% масштабирование и AT остаются отдельными пунктами D9.

Независимое ревью D4.1 с чистым контекстом повторно проверило исходные и конечные шрифты, shaping 1200 строк, лицензии, 46 цветов, FontFace и RTL. Допуск дан, открытых существенных замечаний нет.

## D4.2: controls и адаптивная оболочка

Реализованы нативные radio/checkbox, Button/IconButton, TextAnswer, статусы, source card и таблица, пять основных пунктов и отдельные настройки. Четыре диапазона ширины используют CSS viewport, safe-area сохраняется на каждом; при экранной клавиатуре/малой высоте нижняя навигация переходит в поток. Шапка и клавиши переносятся при крупном тексте. Диалог на узком экране использует showModal/inert; широкий контекст — немодальный region. CSS-переходы не управляют учебными записями, закрытые controls сразу inert, reduced motion убирает движение.

TDD: два исходных браузерных сценария сначала упали на отсутствии компонентов. Исправлены актуальность нативного выделения и повторный change при blur после виртуальной вставки. Ввод заменяет UTF-16 диапазон, возвращает выделение при отмене, сохраняет нативное управление IME; виртуальная вставка не превышает4096. ArabicText внутри label/button не создаёт вложенной кнопки при ошибке шрифта; повтор доступен внешнему ArabicFontGate.

Независимый ревьювер с чистым контекстом обнаружил потерю scroll при смене CSS до layout effect, скрытый инициатор фокуса, безопасные отступы landscape и подавление Enter композиции. Исправления и регрессии внесены. Дополнительная независимая проверка: keyboard Tab/Enter, backward-selection/undo, лимит4096, 200% текста на320/360/640/1120. Основной набор Playwright6/6, TypeScript, границы модулей, исходные валидаторы и build проходят. Подробности визуальной сверки — [ui-implementation.md](ui-implementation.md). Реальный системный IME/AT и физические устройства остаются внешней частью D9.

Финальное независимое ревью D4.2: допуск дан, существенных открытых замечаний нет. Дополнительно исправлено повторное открытие прокрученной панели: новый явный вход начинается с видимого h2 и scroll0, смена ширины текущей панели сохраняет scroll. Последний production build: `1.0.0-9f17248d0f9ee54a`, initial JS92.10kB gzip, CSS3.44kB gzip, полный набор2 756 481байт. Дальше D5: подключение состояния приложения, учебного цикла и чтения.

## D5.1: запуск, владелец записи и выбор пути

Подключены согласованные снимки репозитория, сохранённые настройки, явная передача права записи, восстановление закрытого соединения и отдельная ветвь в памяти. Web Lock не позволяет клонированной вкладке наследовать writer ID из sessionStorage; без locks используется новый ID и явный takeover. Повторные focus/visibility события перечитывают БД. Выбор пути записывается до перехода; сами уроки и упражнения подключаются в D5.2.

До создания БД проверяются версия приложения, минимальный reader, схемы, base path, политики и hashes каталога текущего выпуска. Чтение JSON ограничено по размеру потока. Core validator выделен из полного набора схем: initial JS172.74kB gzip укладывается в180KiB. Полный протокол immutable releases и PWA остаётся D8. Добавлена татарская строка временного хранения; runtime-copy269 строк требует внешней вычитки.

Ревьювер с чистым контекстом обнаружил обновление полномочий старых callbacks, отсутствие minimum-reader fence и неработающий retry закрытого соединения. Исправлены: обязательный CommandScope захватывает repository+Expected; повтор переоткрывает недоступную БД; несовместимый reader не допускается к IDB. Регрессии подтверждают отказ старого callback после reset/memory/reopen и сохранение данных после открытия.

Проверки:143 unit tests,10 Chromium E2E, TypeScript, исходные валидаторы, границы модулей, build и hashes проходят. В браузере проверен реальный onboarding и клонированные вкладки. Production artifact `1.0.0-0e8a44ffa700292e`,24 ресурса,3 757 277байт; выпуск не опубликован. Финальное независимое ревью D5.1: допуск дан, существенных открытых замечаний нет.

## D5.2: полный цикл урока

Теория, правила, демонстрационные и отдельно раскрываемые reference-примеры используют общий каталог. Assessment_source в галерее отсутствуют. Экран практики проходит весь сохранённый план: ввод/выбор → сохранённая отправка → разбор → acknowledgement либо отдельный retry → следующий вопрос → результат. Проверены V04, B01 и A02 с9 practice. Результат различает первую и последнюю попытки, самостоятельность и проработанность; новый цикл начинается явным действием.

Hash routing переведён на data router с ожиданием flush/pause перед полным переходом и ленивыми экранами. Прямой адрес не создаёт Session; onboarding сохраняет проверенное намерение. Предъявление и помощь раскрываются после записи. Сбой Noto при восстановлении сохраняет paused draft. Read-only вкладка может выйти, takeover/retry создают новый редактор с захваченными правами и сохранённым raw draft. При нечитаемой исходной БД временная ветвь пуста, а несохранённый ввод остаётся отдельным видимым полем для копирования. Это не переносит его в выдуманный сеанс.

Смысловые позиции главы привязаны к общему списку якорей для записи и импорта; асинхронный пример ожидается до однократного восстановления. Ошибка загрузки lazy-экрана имеет татарское объяснение. UI-copy279 строк;10 новых строк относятся к оглавлению, сохранению, восстановлению и разбору, внешняя вычитка не заявлена.

Независимый ревьювер с чистым контекстом выявил и перепроверил: roundtrip новых якорей, выход read-only, отказ memory recovery, устаревший editor после takeover, retry show, фокус результата и гонку A/B при отмене draft queue. Исправления сопровождаются регрессиями. Дополнительно проверены pause при отказе шрифта и ожидание EX-якоря после async disclosure. Финальный допуск дан, открытых существенных замечаний нет.

Проверки:147 unit tests,20 Chromium E2E (устаревший locator startup уточнён до h1 и перепроверен), TypeScript, валидаторы, границы, сборка и integrity. Реальные системные IME/AT остаются D9. Визуально проверены1366×900 и360×800; CTA48px, без горизонтального переполнения/ошибок console. Стартовый JS166.74kB gzip: runtime использует компактный индекс assets вместо дублирования всех revisions; полный release manifest сохранён. Build `1.0.0-81019f3e163e8ca2`,27 ресурсов,3 658 324байта. Полные каталоги и оценки — D6, word reader — D5.3, PWA — D8.

## D5.3: чтение и точная словарная связь

READ-01 использует локальные сведения line.words при null lexicon_id; READ-03-L01:0 ведёт в lex-55-044. Каждая строка сохраняет все исходные символы и единый логический RTL-порядок. Выделение не запускает панель; клавиатурный список содержит только поверхности точных вхождений. Четыре уровня помощи раскрываются отдельно после observe, самостоятельный режим скрывает чтение/значение без отмены помощи. Размер арабицы, закладка слова и факт прочтения сохраняются отдельными командами. Итоговые тексты и закрытые COURSE-записи защищены до добавления содержимого в DOM.

Панель использует push/replace и проверяемую собственную предыдущую запись для закрытия. Статья возвращает к панели независимо от порядка query, сохраняются режим, уровни, фокус и прокрутка. При незаконченной проверке подтверждение временно снимает контекстный слой; отмена оставляет помощь скрытой. Закрывающийся слой сразу inert/aria-hidden, CSS-переход не оставляет его в доступном дереве.

Независимый ревьювер с чистым контекстом выявил потерю режима, зависимость возврата от порядка query, потерю keyboard trigger, наложение меню, преждевременную прокрутку до загрузки букв и гонку фокуса с at-якорем. Исправления перепроверены реальным Chromium, включая два RAF и сохранение нативного Tab-порядка. Финальный допуск дан, открытых существенных замечаний нет.

Проверки:149 unit tests,28 уникальных Chromium-сценариев (последний целевой прогон reader/controls/lesson20/20 после исправлений), типы, валидаторы, границы и production integrity проходят. Визуальная сверка1366×900/390×844; runtime на320/390/768/1366px без горизонтального переполнения. Build `1.0.0-ad72383af15fc223`,31 ресурс,3 674 321байт. Runtime-copy283 строки; внешняя вычитка не заявлена. Полные каталоги, RQ и источники остаются плановым D7, оценки — следующим D6. Сквозной срез подтвердил повторное использование форматов; основные инженерные риски дальнейших этапов — несколько вкладок и обновления PWA, внешние рецензии/пилот остаются открытыми условиями, календарные обещания до них не уточняются.
