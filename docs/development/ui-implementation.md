# Визуальная реализация

Канонический источник — DESIGN.md и 04/07. Визуальный ориентир показывает учебную главу: шапку, открытый документ, пример, поле с татарскими клавишами и соседний контекст. Его иллюстративные формулировки не заменяют подготовленный корпус.

Инвентарь D4: Inter 400/600; Noto Naskh Arabic Regular; точные 23 цвета каждой темы; размеры из токенов; Button/IconButton, нативные radio/checkbox, TextAnswer с шестью клавишами и отдельным +, постоянные статусы, именованные диалог/панель, source card, таблица. Связные арабские слова остаются текстом; иллюстраций и растрового текста в продукте нет.

Шапка и пять пунктов следуют 05: Дәресләр, Кабатлау, Уку, Сүзлек, Белешмә; логотип ведёт домой, настройки отдельно. Это сознательное уточнение иллюстративного ориентира по каноническому маршрутизатору. Основное действие, подписи и учебный текст берутся из runtime-каталога и корпуса. Обложки, показатели без истории и декоративные бейджи не требуются для учебного сценария.

Контейнер: 1280px; документ до 680px, панель 320px, зазор 24px. Теория открытая, рамка используется у примера/задания/поля. Цветовая основа тёплая #F7F4ED, без фоновых эффектов. Контрольные точки визуального сравнения: палитра, шрифты/вес, геометрия документа/панели, 48px controls, отсутствие обрезанных арабских marks, адаптивное размещение навигации.

Полная проверка соответствия и сценариев фиксируется по мере подключения экранов; матрица шрифтов D4.1 не выдаётся за готовый учебный экран.

## Сверка D4.2

Обзор компонентов открыт во встроенном браузере, снимки Chromium проверены на 1440×1080, 390×844 и 320px с корневым текстом200%. Сопоставлены шесть пунктов: точные тёплый фон/поверхность/зелёный, Inter/Noto и настоящий вес, ширина документа680px и отступы, рамки примера/поля без декоративных эффектов, 48px ввод/клавиши, навигация и модальная панель на телефоне. Арабские строки остаются настоящим текстом и не получают цвет результата.

Исправлены вертикальное разбиение логотипа при200%, неудачный перенос последней буквы нижнего пункта и скриншот промежуточного кадра диалога: финальный снимок снимается при opacity1. Нижние пункты делят ширину с учётом длины татарской подписи, каждый имеет доступную цель минимум44px. По сравнению с ориентиром названия навигации и теории взяты из канонических05/UI-copy; величины шрифтов/документа — из04, иллюстративный масштаб картинки не заменяет их. Соседнее оглавление появляется по запросу, как требуетS04. Полный корпус подключается далее, поэтому этот обзор не является снимком завершённого урока.

Добавлены две необходимые строки ввода: `keyboard.finish_composition`, `keyboard.answer_limit`. Теперь runtime-каталог содержит268 строк; обе передаются на внешнюю контекстную вычитку вместе с исходными266. Сценарий компонентов: выделение→вставка→undo, запрет вмешательства в composition, Enter без двойной отправки, открытие источника→смена модальности с сохранением фокуса/прокрутки→закрытие.

## Сверка D5.2

Открыт настоящий V04; проверены широкая1366×900 и узкая360×800 главы и узкая практика. Сохранены тёплая открытая колонка680px, Inter/Noto, текстовые правила/источники и спокойные рамки задания/поля. Растрового текста нет. Полная навигация свёрнута на практике, выход сохраняет работу. Исправлен размер ссылки-CTA: она использует ту же минимальную высоту48px, что кнопки. Полные арабские фразы из смешанного текста остаются единым RTL-фрагментом вместе с внутренними пробелами.

Ввод не получает автоматический фокус на телефоне; новый вопрос получает фокус условия, результат — короткого заголовка разбора. Отказ шрифта сохраняет паузу, скрытые им примеры не добавляют exposure до готовности. Смысловой EX-якорь восстанавливается после асинхронного раскрытия. Проверка скриншотов дополняет runtime-тесты и не заменяет реальные устройства/AT. После D5.1/5.2 runtime-copy279 строк, требующих контекстной вычитки.

## Сверка D5.3

READ-03 открыт во встроенном браузере; просмотрены финальные1366×900 и390×844 рендеры. На широком экране документ680px и панель320px разделены24px; на телефоне один модальный слой со спокойным затемнением. Арабская строка остаётся единым текстом, точные вхождения дополнительно доступны через48px кнопки клавиатурного списка. Помощь не открывает перевод всего текста автоматически. Четыре ширины320/390/768/1366 проверены без горизонтального переполнения и ошибок pageerror. Режим, query-якорь, позиция панели и фокус исходной кнопки переживают переход в статью и возврат. Добавлены четыре необходимые подписи, runtime-copy283 строки передаются на внешнюю вычитку.

## Сверка D6.1

Программа использует открытый документ с разделителями,8 раскрываемых модулей и обычные строки уроков. Просмотрены1366×900 и390×844; проверены четыре ширины при100/200% текста. Длинные татарские названия модулей переносятся и после загрузки Inter; названия с арабицей используют MixedText/tt-Arab. Главная на широком экране разделяет продолжение и краткие показатели, на узком сохраняет последовательный поток. Проработанность, самостоятельность и необходимость обновления показаны текстом; полоса повторяет только число проработанных уроков. Runtime-copy288 строк.

## Сверка D6.2

Диагностика открыта во встроенном браузере. Просмотрены1366×900 и390×844; четыре ширины320/390/768/1366 при100/200% тексте не дают горизонтального переполнения. Условия получают фокус без автоматического открытия клавиатуры; разрешённые RQ-строки стоят перед вопросом. Нейтральная отметка в списке означает заполненный черновик, не правильность. Первая и последняя попытки, самостоятельность, отложенная imla и неполные сведения имеют отдельные подписи. Сохранены типографика Inter/Noto, тёплые поверхности, ink для арабицы и48px controls. Runtime-copy310 строк; проверка на физических устройствах и вычитка не подменены Chromium.

## Сверка D7.1

Справочник букв открыт во встроенном браузере. Просмотрены широкий каталог чтения, узкие буквы/профиль/источник и страница правила. Каталог на широком экране использует две колонки, текст правила — документ680px, формы букв — подписанные группы. Проверены5 страниц на320/390/768/1366 при100/200% тексте. Длинные названия источников переносятся, таблица имеет собственный подписанный scroll region. Исходные арабские формы и ZWJ не превращены в новые буквы. Возврат к источнику сохраняет фокус word-панели; оглавление доступно до подтверждения учебной помощи. Runtime-copy339 строк передаются на внешнюю вычитку.


## Сверка D7.2

Поиск сохраняет колонку680px, явные подписи фильтров и раздельные категории обычных/близких/смысловых совпадений. Группы источников раскрываются нативным summary, каждая статья имеет точный адрес. Просмотрены1366×900 и390×844; поиск, полный список и source-only статья проверены на320/390/768/1366 при100/200% текста. Прокрутка списка не сбрасывается при добавлении30 групп. Escape сохраняет поле, возврат из статьи восстанавливает видимый исходный результат. Runtime-copy355 строк ожидает внешней вычитки.


## Сверка D7.3a

RQ сохраняет общий шаблон практики с точным контекстом строки до вопроса, кириллическим вводом и6 клавишами. Выход возвращает к тексту, результат отделяет обычный счёт от самостоятельных ответов и отметки прочтения. Просмотрены390×844 и1366×900; вопрос и раскрытый результат проверены на320/390/768/1366 при100/200% текста. Runtime-copy359 строк.


## Сверка D7.3b

Очередь показывает одну текущую цель и отдельное управление списком, без штрафной окраски просрочки. Раннее повторение доступно только будущим карточкам. Номер вопроса различает карточки одного навыка; условия не раскрываются в управлении. Просмотрены широкий список и узкий результат; очередь, вопрос и результат проверены на320/390/768/1366 при100/200% текста. Длинные татарские подписи переносятся. При всех пропусках нет пустого блока разбора. Runtime-copy373 строки.
